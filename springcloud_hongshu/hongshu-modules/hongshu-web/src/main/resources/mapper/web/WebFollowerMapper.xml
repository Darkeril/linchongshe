<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hongshu.web.mapper.web.WebFollowerMapper">

    <select id="countByFidAndCreateTimeRange" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM web_follower
        WHERE fid = #{fid}
          AND create_time &gt;= #{startTime}
          AND create_time &lt;= #{endTime}
    </select>

</mapper>
